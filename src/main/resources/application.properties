spring.application.name=ai-resume

# OpenAI API key — placeholder so the app can boot during setup.
# TODO: replace with a real key (or set the SPRING_AI_OPENAI_API_KEY env var) before calling any AI features.
spring.ai.openai.api-key=${SPRING_AI_OPENAI_API_KEY:placeholder-set-real-key-later}
